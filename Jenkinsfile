pipeline {
    agent any

    stages {
        stage('Run Command') {
            steps {
                script {
                    // Replace this command with any Linux command you want to run
                    sh 'echo "Hello, World!"'
                }
            }
        }
    }
}
